# Base URL for the API.
# Dev: leave this unset — Vite's proxy (see vite.config.js) forwards /api to
#      http://localhost:5000.
# Prod: set to the API origin including the /api prefix, e.g.
#      VITE_API_URL=https://api.example.com/api
VITE_API_URL=
